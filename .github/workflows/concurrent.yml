name: concurrent workflow
on: 
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    concurrency: 
      group: deploy
      cancel-in-progress: true

    steps:
      - name: checkout
        uses: actions/checkout@v6.0.2
      - name: build app
        run: echo "Concurrent concept implemented"
